package com.util.load;

import java.io.File;

public class RecordLoad {

	public void recoreLoad(String fileName)
	{
		
		File  file = new File(fileName);
		if(file.isFile() && file.exists())
		{
		//	FileInputStream file
			
		}
	}
	
}
